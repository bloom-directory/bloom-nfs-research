# Kerberos/NFS secrets — never commit
*.keytab
*.env
# evidence: keep text transcripts (research record), ignore large pcaps
*.pcap
evidence/**/*.pcap
# agent / local config
.claude/
# editor / OS
.DS_Store
*.swp
